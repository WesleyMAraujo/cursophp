<?php 

    /* 
    abs() valor absoluto ou seja, sem sinal
    base_converter(255, 10, 2) converte para outras bases neste seria 255 em decimal para binario
    ceil() arredonda pra cima
    floor() arredonda pra baixo
    roud() arrendondamento aritmetico
    hypot() calcula a ipotenusa
    intdiv() divisão inteira
    min() e max() minimo e maximo de uma sequencia
    pi() valor de pi
    pow() potencia ou num1**num2
    sin(), cos(), tan() seno cosseno e tangente
    sqrt() raiz quadrada ou num**(1/2)
    */
    
    $vetor = [1,5,6,7,8,9,9,0];
    $res = min($vetor);

    echo "<h1>$res</h1>";
?>